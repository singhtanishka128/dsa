/* notes:

infix = operator btw operands
prefix = .. before ..
postfix = .. after ..

same same but different
infix to postfix, infix to prefix 
postfix to infix, prefix to infix, postfix to prefix, prefix to postfix



iterates left to right: top2 first 
               postfix to infix
               postfix to prefix


iterates right to left: top1 first
              prefix to infix
              prefix to postfix

*/